// Firebase Admin SDK - for server-side operations (API routes, server components)
// To use this, install firebase-admin: npm install firebase-admin
// And add FIREBASE_SERVICE_ACCOUNT_KEY to your .env.local

// import { initializeApp, getApps, cert } from "firebase-admin/app";
// import { getAuth } from "firebase-admin/auth";
// import { getFirestore } from "firebase-admin/firestore";
//
// const serviceAccount = JSON.parse(
//   process.env.FIREBASE_SERVICE_ACCOUNT_KEY || "{}"
// );
//
// const adminApp =
//   getApps().length === 0
//     ? initializeApp({ credential: cert(serviceAccount) })
//     : getApps()[0];
//
// const adminAuth = getAuth(adminApp);
// const adminDb = getFirestore(adminApp);
//
// export { adminApp, adminAuth, adminDb };

// Uncomment the above when you're ready to use Firebase Admin SDK
export { };
